# Where the Vite dev/preview server forwards /api requests (your FastAPI app).
VITE_API_TARGET=http://127.0.0.1:8000

# Base URL the browser uses. Leave as /api to use the proxy (recommended).
# A full URL such as http://127.0.0.1:8000 only works if the backend allows CORS.
VITE_API_BASE=/api
